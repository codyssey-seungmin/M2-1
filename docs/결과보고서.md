# Mindily 결과보고서

**코디세이 AI 네이티브 Final Project (자율 주제 · 240시간)**

| | |
|---|---|
| 서비스명 | **Mindily** — 감정을 기록에서 끝내지 않는 AI 감정 코치 |
| 배포 URL | https://huggingface.co/spaces/yellowmug/mindily (아래 5.4절 참조) |
| 저장소 | https://github.com/80gina/MM |
| 팀 | 3인 (기획·개발·검증) |
| 작성일 | 2026-09-19 |

---

## 한눈에 보기

> **일기를 쓰면 실제 감정 분류 모델이 감정을 읽고, 생성형 AI가 검색된 출처 안에서만 코치 문장을 만들어, 근거가 있는 회복 활동까지 연결합니다.**

| 평가 항목 | 구현 | 확인 방법 |
|---|---|---|
| 생성형 AI 또는 AI Agent가 핵심 기능 | ✅ **둘 다** — 도구 호출형 Agent + 출처 접지 생성형 응답 | `GET /api/llm/status`, `evidence/llm-check.json` |
| 필수 기술 요소 2개 이상 | ✅ **4개** — Agent · RAG · 생성형 AI · Long-term Memory | 아래 2.2절 |
| 외부 접근 가능한 배포 | ✅ **https://yellowmug-mindily.hf.space** 상시 동작 (설치형 웹앱) | 아래 5.4절 |
| 실사용자 5명 이상 테스트·피드백 반영 | ⬜ **유일한 미완료 항목** (6절에 결과 기입) | `docs/피드백기록.md` |
| GitHub 버전 관리 기록 | ✅ 63 커밋, 기능별 분리 | 커밋 히스토리 |
| AI 윤리 (생성 명시·동의·최소 수집) | ✅ | 아래 4절 |

---

## 1. 문제 정의

### 1.1 우리가 본 공백

감정 관리 서비스는 두 갈래로 나뉘어 있고, 그 사이가 비어 있습니다.

| 유형 | 하는 일 | 못 하는 일 |
|---|---|---|
| 기록형 앱 | 감정 이모지를 고르고 저장한다 | "그래서 지금 무엇을 해야 하는가"에 답하지 못한다 |
| 대화형 챗봇 | 그럴듯한 위로 문장을 만든다 | 근거가 없어 사용자가 신뢰 여부를 판단할 수 없다 |

### 1.2 우리가 정의한 문제

> **"오늘 내 감정이 무엇인지"에서 멈추지 않고, "그래서 지금 15분 동안 무엇을 하면 되는지"까지 출처와 함께 이어주는 서비스가 없다.**

이 문제를 풀려면 세 가지를 동시에 만족해야 했습니다.

| 요구 | 왜 어려운가 |
|---|---|
| 감정 판정이 **검증 가능**해야 한다 | LLM에게 감정을 물으면 그럴듯한 답은 나오지만 근거를 제시할 수 없다 |
| 추천에 **출처**가 있어야 한다 | 생성형 AI는 존재하지 않는 활동·효능을 만들어낼 수 있다 |
| 개인화하되 **개인정보를 최소로** 다뤄야 한다 | 감정 일기는 가장 민감한 개인 기록이다 |

### 1.3 범위 밖으로 명시한 것

의료적 진단·치료·처방, 위기 상담 대체, 일기 원문의 서버 저장 및 학습 이용 — 세 가지는 명시적으로 하지 않습니다.

---

## 2. 해결 설계

### 2.1 전체 구조

![시스템 아키텍처](architecture.svg)

```
일기 입력 → [Agent] 의도 판별
              ├─ analyze_emotion    → KcELECTRA 실제 추론 → 감정 점수
              ├─ retrieve_grounding → 출처 카드 검색 (RAG)
              ├─ generate_coach_message → 생성형 AI, 출처 안에서만 문장 생성
              └─ recommend_healing  → 출처 링크가 붙은 활동 카드
           → 사용자가 무드 미터에서 최종 감정 확정
           → (동의 시) 선호 활동 30일 기억
           → 기록 누적 → 주/월/연 흐름 그래프와 감정 분석 보고서
```

일기 원문과 기록은 **기기의 브라우저 안에만** 남습니다. 그래프와 보고서도 서버를 거치지 않고 그 기록만으로 그립니다.

### 2.2 필수 기술 요소 — 요구 2개, 구현 4개

| 요소 | 구현 위치 | 핵심 내용 | 검증 |
|---|---|---|---|
| **AI Agent** | `coach_agent.py` | 일기/대화/휴식요청 의도를 판별해 필요한 도구만 순서대로 호출. 실행 추적에 도구 이름만 기록 | `test_agent.py`<br>`evidence/agent-check.json` |
| **RAG** | `rag.py`<br>`healing_knowledge.py` | 감정·스트레스·가용 시간으로 활동 카드를 검색하고 출처·라이선스를 함께 반환 | `test_rag.py`<br>`evidence/rag-check.json` |
| **생성형 AI** | `llm.py` | 코디세이 제공 API(OpenAI 호환)에 **출처 카드만** 전달해 코치 문장 생성. 일기 원문 미전송, 실패 시 자동 폴백 | `test_llm.py`<br>`evidence/llm-check.json` |
| **Long-term Memory** | `memory_db.py` | 동의 기반 선호 활동 저장, 30일 자동 만료, 사용자 삭제 | `test_memory_restart.py`<br>`evidence/memory-retention-check.json` |

### 2.3 기술적 의사결정과 근거

평가에서 가장 중요한 부분이라 판단해 **선택하지 않은 대안과 그 이유**를 함께 적습니다.

| # | 결정 | 검토한 대안 | 선택 근거 |
|---|---|---|---|
| 1 | 감정 분류에 **전용 모델(KcELECTRA)** 사용 | LLM에게 감정 추정을 요청 | LLM 추정은 근거를 제시할 수 없다. 분류 모델은 **점수와 모델 버전을 공개**할 수 있어 제3자가 재현·검증할 수 있다 |
| 2 | 생성형 AI를 **문장 작성에만** 사용 | 분석부터 추천까지 LLM에 일임 | 판정은 검증 가능해야 하고 문장은 자연스러워야 한다. **역할을 분리**해 각 층의 강점만 쓴다 |
| 3 | LLM에 **일기 원문을 보내지 않음** | 원문을 그대로 전달해 공감도 향상 | 감정 일기는 가장 민감한 기록이다. 감정 라벨·스트레스·출처 카드만으로도 충분한 품질을 확인했고, 유출 경로 자체를 제거하는 편이 낫다고 판단 |
| 4 | 생성 실패 시 **결정론적 폴백** | 오류 화면 표시 | 외부 API 장애가 서비스 중단이 되어서는 안 된다. 폴백 여부는 `generation` 필드로 **항상 공개**한다 |
| 5 | 일기 원문 **브라우저 보관** | 서버 DB 저장 | 서버 유출 위험 자체를 제거. 개인정보 최소 수집 원칙 |
| 6 | 선호 기억 **30일 만료** | 무기한 보관 | 개인화와 보호를 동시에 증명. 만료·삭제를 테스트로 검증 |
| 7 | 배포에 **Hugging Face Spaces** | Render 유료 플랜 | 모델이 HF에 있어 캐시가 빠르고, 무료 등급으로 **영구 HTTPS URL** 확보. 모델을 이미지에 포함해 콜드스타트 제거 |
| 8 | 프론트엔드 **순수 HTML/JS** | React 등 프레임워크 | 빌드 체인 없이 정적 배포, 팀 전원이 직접 수정 가능 |
| 9 | **설치형 웹앱(PWA)** | 네이티브 앱 스토어 등록 | 등록비·심사 없이 홈 화면 설치와 오프라인 화면을 확보. 화면 자원만 캐시하고 `/api/*`는 캐시하지 않아 분석 결과가 낡지 않는다 |
| 10 | 기록 그래프의 세로축을 **스트레스**로 | 모델 분류 점수를 높이로 | 분류 점수는 높다고 좋은 것이 아니라 거꾸로 읽히기 쉽다. 스트레스는 사용자가 직접 기록한 값이라 해석이 어긋나지 않는다 |
| 11 | 연간은 **월 평균 12칸** | 365일을 하루씩 표시 | 모바일에서 365개 막대는 읽을 수 없다. 기록이 없는 달은 빈칸으로 두고 0으로 채우지 않는다 |
| 12 | 자연의 소리를 **브라우저에서 합성** | 음원 파일 동봉 | 저작권 문제와 용량 증가를 동시에 없앤다 (Web Audio API) |

---

## 3. 생성형 AI 통합 상세

### 3.1 접지(grounding) 방식

생성형 AI가 자유롭게 답하면 존재하지 않는 활동이나 의료적 조언을 만들어낼 수 있습니다. 이를 **세 겹으로** 막았습니다.

| 층 | 방법 |
|---|---|
| 입력 제한 | 프롬프트에 검색된 출처 카드 3장만 넣는다. 일기 원문은 넣지 않는다 |
| 지시 제한 | "카드에 없는 활동을 만들지 않는다 / 진단·처방·병원 권유 금지 / 감정 단정 금지 / 2~3문장" |
| 출력 검사 | 생성 문장에 금지어(진단·처방·치료·우울증 등)가 있으면 **폐기하고 규칙 기반으로 폴백** |

### 3.2 응답 경로 공개

모든 응답은 어느 경로로 만들어졌는지 스스로 밝힙니다.

| `generation` 값 | 의미 | 사용자 화면 표시 |
|---|---|---|
| `llm_grounded` | 생성형 AI가 출처 안에서 작성 | "AI가 생성한 문장이며 의료적 진단이 아니에요" |
| `deterministic_fallback` | 규칙 기반 문장 | "코치 문장은 규칙 기반이에요" |

폴백 사유(`not_configured` / `call_failed` / `safety_filter`)도 응답에 포함되어 운영 중 진단이 가능합니다.

### 3.3 실제 동작 예 (배포 환경 실측)

**입력**: `내일 발표를 잘할 수 있을지 걱정돼요.` (스트레스 3)
**측정**: 2026-09-21, `https://yellowmug-mindily.hf.space/api/agent/coach` 직접 호출

| 단계 | 결과 |
|---|---|
| `analyze_emotion` | **불안 0.980** · 1청크 · 모델 리비전 `2eaf89d8` 고정 |
| `recommend_healing` | 1분 호흡하기 · 주변 감각 돌아보기 · 빗소리 듣기 |
| `retrieve_grounding` | 위 활동의 출처 카드 3장 (NHS · Kent NHS · MDN) |
| `generate_coach_message` | **completed** — 카드 안에서 생성 |
| 최종 응답 | `generation: llm_grounded` · 총 11.9초 |

생성된 문장(실제 응답 그대로):

> 지금 조금 불안하게 느껴지실 수 있어요. 편안한 자세에서 무리하지 않고 천천히 숨을 들이쉬고 내쉬는 1분 호흡하기를 해보세요.

감정을 단정하지 않는 어미(`느껴지실 수 있어요`)와 **출처 카드에 있는 활동만** 언급하는 제약이 실제 출력에서 지켜졌습니다.

#### 연동 과정에서 실제로 겪은 문제 (기록)

| 증상 | 원인 | 조치 |
|---|---|---|
| 모든 호출이 `fallback_reason: call_failed` | 추론형 모델(`gpt-5-mini`)은 `temperature` 변경을 거부하고 `max_tokens` 대신 `max_completion_tokens`를 받는다 | 모델 계열에 맞는 요청 본문을 먼저 보내고, 거절되면 다른 규격으로 재시도 |
| 200 응답인데 본문이 빈 문자열 | 추론형 모델이 **보이지 않는 추론 토큰**을 먼저 소비해 한도(512)를 다 씀 | 한도를 1400으로 올리고 `reasoning_effort`를 낮춤 |
| 원인 파악이 불가능 | 예외를 전부 삼켜 폴백만 남음 | `/api/llm/status`에 `last_error`를 공개(키는 미포함)해 **배포 환경에서 원인 진단 가능** |

위 측정값은 `evidence/deploy-check.json`에 그대로 저장했습니다.

이 과정에서도 사용자 화면은 한 번도 오류를 보이지 않았습니다. 폴백 설계가 의도대로 동작한 실증 사례입니다.

---

## 4. AI 윤리 및 개인정보

### 4.1 데이터 처리

| 데이터 | 저장 위치 | 보존 | 동의 | 외부 전송 |
|---|---|---|---|---|
| 일기 원문 | 브라우저 localStorage | 사용자 삭제 시까지 | 불필요 | **없음** (LLM에도 미전송) |
| 확정 감정·스트레스 | 브라우저 localStorage | 동일 | 불필요 | 없음 |
| 선호 활동 | 서버 SQLite | **30일 자동 삭제** | **필수** | 없음 |
| 만족도·의견 | 서버 SQLite (익명) | 과제 종료 시 파기 | **필수** | 없음 |

### 4.2 지킨 원칙

- AI가 생성·추천한 콘텐츠임을 **화면에 표시**합니다.
- 동의를 거부해도 **핵심 기능을 그대로** 사용할 수 있습니다.
- 감정 분류 점수를 **진단이나 감정 구성비로 표시하지 않습니다**. 최종 감정은 사용자가 확정합니다.
- Agent 실행 추적에 **도구 이름만** 남기고 일기 원문을 넣지 않습니다.
- 필사·음악 콘텐츠는 **저작권 만료 또는 공개 라이선스** 자료만 사용하고 긴 원문을 저장하지 않습니다.

---

## 5. 구현 및 검증

### 5.1 기술 스택

| 영역 | 선택 |
|---|---|
| 감정 분류 | KcELECTRA (`GGARA02/kcelectra-korean-emotion`, 리비전 `2eaf89d8` 고정) |
| 생성형 AI | **코디세이 제공 API** (OpenAI 호환), 출처 접지 + 안전 필터 + 자동 폴백 |
| 백엔드 | FastAPI · Uvicorn · PyTorch (CPU) |
| 프론트엔드 | HTML · CSS · JavaScript (`dist/`, 빌드 체인 없음) |
| 앱 형태 | 설치형 웹앱(PWA) — `manifest.webmanifest` + 서비스 워커. 홈 화면 설치, 화면 자원만 캐시 |
| 저장 | 브라우저 localStorage + SQLite |
| 배포 | Docker → Hugging Face Spaces (CPU basic) |

### 5.2 검증 결과

| 대상 | 테스트 | 확인 내용 |
|---|---|---|
| 감정 분석 API | `test_api.py` | 정상·오류·1000자 초과 청크 처리 |
| Agent | `test_agent.py` | 의도별 도구 호출 순서, 추적에 원문 미포함 |
| RAG | `test_rag.py` | 감정별 출처 카드 검색, 출처 필드 존재 |
| **생성형 AI** | `test_llm.py` | **프롬프트에 일기 원문 미포함**, 키 없을 때 폴백, 금지어 차단, 상태 응답에 키 미노출 |
| Long-term Memory | `test_memory_restart.py` | 프로세스 재시작 후 유지, 30일 만료, 삭제 |
| 피드백 | `test_feedback.py` | 동의 없으면 미저장, 점수 범위 검증 |
| 패키징 | `test_packaging.py` | Docker 이미지에 로컬 모듈 누락 없음 |
| **설치형 앱(PWA)** | 브라우저 실동작 | 서비스 워커 등록, 화면 자원 캐시 9종, **오프라인 재접속 시 화면 정상 표시**, `/api/*` 무캐시 |
| 화면 회귀 | `getElementById` 참조 대조 | HTML에 없는 요소 참조 0건 — 아래 회귀 기록 참조 |
| **기록 그래프·보고서** | 브라우저 실동작 | 기록 0건/5건/17건, 주간·월간·연간 전환, 보고서 텍스트 저장까지 오류 0건 |
| **배포 환경 생성형 경로** | 실서버 호출 | `generation: llm_grounded` 확인 (3.3절 실측) |

#### 검증 중 발견해 고친 회귀 (기록)

힐링 코치를 탭 구조로 바꾼 커밋(`a1800b4`)에서 **Long-term Memory 동의 카드와 감정 정리 카드가 함께 삭제**되었습니다.
`app.js`가 존재하지 않는 요소를 참조해 예외를 던졌고, 그 아래 코드가 실행되지 않는 상태였습니다.

| 항목 | 내용 |
|---|---|
| 증상 | 필수 기술 요소 중 하나(Long-term Memory)의 UI가 화면에서 사라짐 |
| 원인 | 정적 데모 카드를 제거할 때 인접한 기능 카드까지 같은 블록에서 삭제 |
| 발견 방법 | 브라우저 실행 후 `pageerror` 수집, `app.js`의 `getElementById` 참조를 HTML과 대조 |
| 조치 | 카드 복원(`6ea3d1c`) 후 참조 누락 0건 확인, 회귀 점검을 배포 전 절차에 추가 |

지우지 않고 기록으로 남깁니다. 발견 경로(브라우저 오류 수집 + 참조 대조)를 절차화한 것이 이 건의 실질적 결과입니다.

---

### 5.3 모델 적용 결과

전체 내용은 [모델 적용 결과](모델적용결과.md)에 정리했으며, 핵심만 옮깁니다. **모든 수치는 실측값**입니다.

#### 실제 분류 결과 (`evidence/api-check.json`)

| 입력 | HTTP | 최상위 감정 | 점수 | 청크 |
|---|---|---|---|---|
| 걱정 문장 (`내일 발표를…`) | 200 | **불안** | **0.941** | 1 |
| 긴 글 (128토큰 초과) | 200 | 불안 | 0.437 | **2** |
| 빈 입력 / 범위 밖 스트레스 | 422 | — | — | 입력 검증 차단 |

긴 글에서 점수가 낮아진 것은 오작동이 아니라, 청크별 결과를 토큰 수로 가중 평균해 **단일 감정으로 단정하지 않는** 의도된 동작입니다.

#### 추가 학습 시도와 채택하지 않은 이유

`team_motion`은 AI Hub의 **감정이 태깅된 자유대화 (성인·청소년)**를 보관한 로컬 폴더입니다. 발화 **847,156건**(파일 2,335개)은 이 폴더에서 집계한 수치이며 AI Hub 전체 데이터 규모가 아닙니다. 이 가운데 학습·검증·테스트 각 100건으로 추가 학습을 시험했습니다. 기본 공개 모델의 원 학습 자료인 AI Hub **감성대화말뭉치**와는 별도 데이터셋입니다.

| 문제 | 수치 |
|---|---|
| `상처` 라벨 신규 표본 없음 | 6종 중 1종 누락 → 기존 분류 능력 손상 위험 |
| 극심한 클래스 불균형 | 학습셋 기쁨 330,711 vs 불안 16,233 (**약 20:1**) |
| 제외된 라벨 | 없음 206,219 · 사랑스러움 34,624 (대응 라벨 없음) |

CPU 환경 제약으로 학습 100 / 검증 100 / 테스트 100건, 1에폭 파일럿으로 진행한 결과:

| 구분 | Loss | Accuracy | Macro F1 |
|---|---|---|---|
| 검증 | 1.9917 | 0.24 | 0.2395 |
| **테스트** | **1.8622** | **0.26** | **0.2496** |

5개 클래스 무작위 추측의 기대 정확도가 0.20인데 측정값이 0.26입니다. **무작위보다 의미 있게 낫다고 말할 수 없는 수준**이므로, 서비스에는 **공개 모델을 그대로 적용**했습니다.

> 이 수치는 데이터가 쓸모없다는 뜻이 아니라 **CPU·100건·1에폭 조건에서는 개선을 확인할 수 없었다**는 뜻입니다. 실패한 시도를 지우지 않고 남긴 것도 기록의 일부입니다.

### 5.4 배포 현황

| 항목 | 상태 |
|---|---|
| 서비스 주소 | **https://yellowmug-mindily.hf.space** (Docker SDK · CPU basic · Public) |
| 상태 | ✅ Running — `/api/health` = `ready`, 모델·리비전 일치 |
| 생성형 AI | ✅ `llm_grounded` (코디세이 제공 API, `gpt-5-mini`) |
| 설치형 앱 | ✅ 홈 화면 설치, 오프라인에서도 화면 표시 |
| 소스 | https://github.com/80gina/MM |
| 시연 영상 | `docs/mindily-demo-3min.mp4` (3분 01초) |

초기에는 신규 무료 계정에서 보고되는 플랫폼 오류(`CPU Basic quota limit`)로 컨테이너가 기동하지 않아, 그동안 Cloudflare 임시 HTTPS와 같은 와이파이 주소로 시연·테스트를 진행했습니다. 현재는 해제되어 위 주소가 상시 동작합니다.

**무료 등급에서 알아둘 점**

- 48시간 접속이 없으면 절전에 들어가며, 이후 첫 접속에 1~2분이 걸립니다. 발표 전 미리 깨워 둡니다.
- 영구 디스크가 없어 재시작 시 서버의 만족도·선호 기억이 초기화됩니다. 테스트 직후 집계를 내려받아 보관합니다.
- 일기 원문과 기록 그래프는 원래 기기 안에만 저장되므로 재시작의 영향을 받지 않습니다.

---

## 6. 사용자 테스트 및 개선

> **작성 안내**: 아래 표를 실제 결과로 채웁니다. 이 절이 비면 "만들었지만 써본 사람이 없다"로 읽힙니다.

### 6.1 진행 방법

| 항목 | 내용 |
|---|---|
| 참여자 | **(테스트 후 기입)** — 서로 다른 실사용자 5명 |
| 공통 입력 | `내일 발표를 잘할 수 있을지 걱정돼요.` (스트레스 3) |
| 평가 문항 | 분석 이해도 · 공감도 · 추천 유용성 · 접근성 (각 5점 척도) |
| 동의 | 익명 처리 및 과제 종료 후 파기를 사전 고지 |

### 6.2 결과

| 문항 | 평균 | 비고 |
|---|---|---|
| 분석 이해도 | (점) | |
| 공감도 | (점) | |
| 추천 유용성 | (점) | |
| 접근성 | (점) | |

### 6.3 피드백 반영 (개선 전 → 후)

| # | 피드백 | 원인 | 조치 | 커밋 | 재확인 |
|---|---|---|---|---|---|
| 1 | 만족도 점수를 고르지 않으면 창이 닫히지 않음 | 닫기 버튼이 폼 기본 제출로 처리됨 | 일반 버튼으로 분리 | `d098bdf` | ✅ |
| 2 | (기입) | | | | |
| 3 | (기입) | | | | |

> 위 표는 **실사용자 응답 전용**입니다. 아직 비어 있습니다.

#### 자체 점검으로 고친 항목 (실사용자 응답과 별도)

개발·사용 과정에서 발견해 고친 8건은 `docs/피드백기록.md`에 날짜·원인·수정 커밋·재확인과 함께 기록했습니다. 대표 3건은 다음과 같습니다.

| 발견 | 조치 | 커밋 |
|---|---|---|
| 상위 3개 감정만 보여 점수의 뜻이 읽히지 않음 | 6개 감정 레이더로 교체, 제목을 ‘감정 분류 점수’로 | `7689aba` |
| 필수 기술 요소인 기억 동의 카드가 화면에서 사라짐 | 카드 복원 + 참조 대조를 배포 전 점검 절차에 추가 | `6ea3d1c` |
| 새로 배포해도 브라우저가 옛 화면을 보여줌 | 릴리스 표식과 재검증 헤더 | `3561f00` |

이 8건은 **실사용자 피드백 건수에 포함하지 않습니다.**

---

## 7. 한계와 다음 단계

| 현재 한계 | 영향 | 다음 단계 |
|---|---|---|
| 생성형 응답이 감정 라벨·출처 카드만 참조 (원문 미전달) | 개별 상황 묘사의 구체성이 낮음 | 사용자가 명시적으로 동의한 경우에 한해 원문 전달 옵션 제공 |
| 무료 Space는 영구 디스크가 없음 | 재시작 시 피드백·선호 데이터 초기화 | 수집 즉시 집계 내려받기, 필요 시 유료 디스크 전환 |
| 위치 기반 추천(러닝·근처 장소) 미구현 | F23·F24 범위 밖 | 권한·지도 API 약관 검토 후 2단계 |
| 감정 6종으로 제한 | 세밀한 감정 표현 한계 | 무드 미터에서 사용자 직접 선택으로 보완 중 |
| 추론형 모델의 생성 응답이 느림 (실측 약 12초) | 분석 화면 대기 시간 증가 | 더 가벼운 모델 선택 또는 코치 문장 비동기 로딩 |
| 기록이 기기별로 분리됨 | 폰과 PC의 그래프가 서로 다름 | 계정 없이 유지하려면 내보내기·가져오기 기능이 필요 |

---

## 8. 팀 구성 및 기여

| 역할 | 이름 | 담당 | 대표 커밋 |
|---|---|---|---|
| 기획 / UX | **김다빈** | 서비스 콘셉트, 모바일 UI, 접근성, 사용자 테스트 설계 | [`7689aba`](https://github.com/80gina/MM/commit/7689aba) · [`f164d49`](https://github.com/80gina/MM/commit/f164d49) · [`1148bb4`](https://github.com/80gina/MM/commit/1148bb4) |
| 개발 | **김진아** | 모델 연동, FastAPI, Agent·RAG·생성형 AI·Memory, 배포 | [`4c58d82`](https://github.com/80gina/MM/commit/4c58d82) · [`6ea3d1c`](https://github.com/80gina/MM/commit/6ea3d1c) · [`b77aea5`](https://github.com/80gina/MM/commit/b77aea5) |
| 검증 / 문서 | **안승민** | 기능명세, 테스트 실행, 사용자 피드백 수집·정리 | [`e547f92`](https://github.com/80gina/MM/commit/e547f92) · [`557d1b0`](https://github.com/80gina/MM/commit/557d1b0) · [`30d6917`](https://github.com/80gina/MM/commit/30d6917) |

상세 내역은 [`docs/TEAM_ROLES.md`](TEAM_ROLES.md)를 참고합니다.

---

## 9. 결론

이 프로젝트에서 팀이 가장 신경 쓴 것은 **"AI를 얼마나 많이 썼는가"가 아니라 "어디에 어떤 AI를 썼고 왜 그렇게 나눴는가"** 였습니다.

- 감정 **판정**은 검증 가능해야 하므로 전용 분류 모델에 맡기고, 점수와 모델 버전을 공개했습니다.
- 감정 **표현**은 자연스러워야 하므로 생성형 AI에 맡기되, 검색된 출처 안에서만 말하도록 세 겹으로 제한했습니다.
- **개인정보**는 가장 민감한 부분이므로, 일기 원문이 서버에도 생성형 AI에도 가지 않는 경로를 설계했습니다.

구현하지 못한 것을 구현한 것처럼 적지 않는 것 역시 이 보고서의 원칙입니다. 체크 표시는 실제로 확인한 항목에만 붙였습니다.

---

## 부록

| 문서 | 내용 |
|---|---|
| [기획서](기획서.md) | 문제 정의·타겟·AI 활용·기술 접근·일정 |
| [기능명세서](기능명세서.md) | 기능·비기능 요구사항 |
| [시스템 아키텍처](architecture.md) | 계층 구조와 설계 근거 |
| [모델 적용 결과](모델적용결과.md) | 실제 분류 결과·추가 학습 측정값·채택 판단 |
| [모델학습보고서](모델학습보고서.md) | 전처리·토큰화·학습 설정 상세 |
| [발표자료 구성안](발표자료구성안.md) | 12장 구성과 장별 메시지 |
| [피드백 기록](피드백기록.md) | 사용자 피드백과 개선 전후 |
| [팀 역할](TEAM_ROLES.md) | 역할·커밋·회의 기록 |
